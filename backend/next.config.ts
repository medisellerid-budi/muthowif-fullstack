import type { NextConfig } from "next";

// CORS is handled by src/middleware.ts
// Do NOT add CORS headers here to avoid duplicate header conflict
const nextConfig: NextConfig = {};

export default nextConfig;
